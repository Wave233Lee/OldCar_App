<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.group">

    <uses-permission android:name="android.permission.INTERNET" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" android:launchMode="standard">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".SearchViewActivity" android:theme="@style/AppCompatTheme" />
        <activity android:name=".OldcarHomeActivity" android:theme="@style/AppCompatTheme" />
        <activity android:name=".OldcarFindcarActivity" android:theme="@style/AppCompatTheme" />
        <activity android:name=".OldcarCarActivity" android:theme="@style/AppCompatTheme" />
        <activity android:name=".OldcarSaleActivity" android:theme="@style/AppCompatTheme" />
        <activity android:name=".SearchResultActvity" android:theme="@style/AppCompatTheme" />
        <activity android:name=".widget.View1Activity"/>
        <activity android:name=".widget.AdvActivity"/>
        <activity android:name=".widget.CarActivity"/>
        <activity android:name=".widget.View2Activity"/>
    </application>

</manifest>